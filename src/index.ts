import ReactiveStage from "./InteractiveStage";

export default ReactiveStage;

export type {
  InteractiveStageProps,
  InteractiveStageType,
  InteractiveStageRef,
  InteractiveStageRenderProps,
} from "./types";
